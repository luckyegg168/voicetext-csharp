<!-- src/VoiceText.App/Views/MainWindow.xaml -->
<Window x:Class="VoiceText.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:VoiceText.App.Converters"
        Title="VoiceText" Height="560" Width="440" xml:lang="zh-TW"
        Background="{StaticResource BackgroundPrimaryBrush}"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResize" Topmost="True">

  <Window.Resources>
    <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    <converters:StateToColorConverter x:Key="StateToColorConverter"/>
    <converters:StateToIconConverter x:Key="StateToIconConverter"/>
    <converters:StateToShadowColorConverter x:Key="StateToShadowColorConverter"/>
  </Window.Resources>

  <Window.Effect>
    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.25"/>
  </Window.Effect>

  <Border CornerRadius="16" Background="{StaticResource BackgroundPrimaryBrush}"
          BorderBrush="{StaticResource SeparatorBrush}" BorderThickness="0.5">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="44"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="56"/>
      </Grid.RowDefinitions>

      <!-- Title bar (drag region) -->
      <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <TextBlock Text="VoiceText" Style="{StaticResource Headline}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Content="⚙" HorizontalAlignment="Right" Margin="0,0,44,0"
                Style="{StaticResource AppleButton}" Background="Transparent"
                Foreground="{StaticResource SystemGrayBrush}"
                Command="{Binding OpenSettingsCommand}"/>
        <Button Content="✕" HorizontalAlignment="Right" Margin="0,0,8,0"
                Style="{StaticResource AppleButton}" Background="Transparent"
                Foreground="{StaticResource SystemGrayBrush}"
                Click="CloseButton_Click"/>
      </Grid>

      <!-- Main content -->
      <ScrollViewer Grid.Row="1" Padding="16,0" VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <!-- Record button -->
          <Border HorizontalAlignment="Center" Margin="0,20,0,8">
            <Button Command="{Binding ToggleRecordingCommand}" Width="80" Height="80"
                    Background="Transparent" BorderThickness="0" Cursor="Hand">
              <Button.Template>
                <ControlTemplate TargetType="Button">
                  <Grid>
                    <Ellipse Width="80" Height="80"
                             Fill="{Binding State, Converter={StaticResource StateToColorConverter}}">
                      <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.35"
                                          Color="{Binding State, Converter={StaticResource StateToShadowColorConverter}}"/>
                      </Ellipse.Effect>
                    </Ellipse>
                    <TextBlock Text="{Binding State, Converter={StaticResource StateToIconConverter}}"
                               FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="White"/>
                  </Grid>
                </ControlTemplate>
              </Button.Template>
            </Button>
          </Border>

          <!-- Status -->
          <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}"
                     HorizontalAlignment="Center" Margin="0,0,0,12"/>

          <!-- Polish toggle row -->
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
            <CheckBox Content="自動潤稿" IsChecked="{Binding IsPolishEnabled}"
                      FontFamily="{StaticResource AppleFont}" FontSize="14" Margin="0,0,16,0"/>
            <TextBlock Text="語言:" VerticalAlignment="Center" FontFamily="{StaticResource AppleFont}" FontSize="14" Margin="0,0,6,0"/>
            <ComboBox SelectedValue="{Binding SelectedLanguage}" SelectedValuePath="Content"
                      Width="80" FontSize="13">
              <ComboBoxItem Content="auto"/>
              <ComboBoxItem Content="zh"/>
              <ComboBoxItem Content="en"/>
              <ComboBoxItem Content="ja"/>
              <ComboBoxItem Content="ko"/>
            </ComboBox>
          </StackPanel>

          <!-- Raw text card -->
          <Border Style="{StaticResource AppleCard}" Margin="0,0,0,8">
            <StackPanel>
              <TextBlock Text="語音轉錄" Style="{StaticResource Caption}" Margin="0,0,0,6"/>
              <TextBox Text="{Binding RawText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Style="{StaticResource AppleTextBox}" TextWrapping="Wrap"
                       MinHeight="80" AcceptsReturn="True"/>
            </StackPanel>
          </Border>

          <!-- Polished text card -->
          <Border Style="{StaticResource AppleCard}" Margin="0,0,0,8"
                  Visibility="{Binding IsPolishEnabled, Converter={StaticResource BoolToVis}}">
            <StackPanel>
              <TextBlock Text="潤稿結果" Style="{StaticResource Caption}" Margin="0,0,0,6"/>
              <TextBox Text="{Binding PolishedText, Mode=TwoWay}"
                       Style="{StaticResource AppleTextBox}" TextWrapping="Wrap"
                       MinHeight="80" AcceptsReturn="True"/>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>

      <!-- Toolbar -->
      <Border Grid.Row="2" Background="{StaticResource BackgroundSecondaryBrush}"
              BorderBrush="{StaticResource SeparatorBrush}" BorderThickness="0,0.5,0,0">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Button Content="📋 複製" Style="{StaticResource AppleButton}" Margin="4,0"
                  Background="{StaticResource BackgroundTertiaryBrush}"
                  Foreground="{StaticResource LabelPrimaryBrush}"
                  Command="{Binding CopyToClipboardCommand}"/>
          <Button Content="🌐 翻譯" Style="{StaticResource AppleButton}" Margin="4,0"
                  Background="{StaticResource BackgroundTertiaryBrush}"
                  Foreground="{StaticResource LabelPrimaryBrush}"
                  Command="{Binding TranslateCommand}"/>
          <Button Content="🕐 歷史" Style="{StaticResource AppleButton}" Margin="4,0"
                  Background="{StaticResource BackgroundTertiaryBrush}"
                  Foreground="{StaticResource LabelPrimaryBrush}"
                  Command="{Binding OpenHistoryCommand}"/>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</Window>
